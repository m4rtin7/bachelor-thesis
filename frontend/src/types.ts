export type Excersise = {
  id: number;
  title: string;
  text: string;
  leftVersion: number;
  leftCode: string;
  leftTests: string;
  rightVersion: number;
  rightCode: string;
  rightTests: string;
};
